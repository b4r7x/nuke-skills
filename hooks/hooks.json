{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const p=require('path');console.log(require('fs').readFileSync(p.join(process.env.CLAUDE_PLUGIN_ROOT,'core','references','constitution.md'),'utf8'))\""
          }
        ]
      }
    ]
  }
}
